<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Forgot Password | My Blog</title>
  <link rel="stylesheet" href="/style.css" />
  <style>
    body {
      font-family: "Poppins", sans-serif;
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: #02b7ff;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
  background: url("https://images.unsplash.com/photo-1584820927498-cfe5211fd8bf?auto=format&fit=crop&w=1950&q=80")
              no-repeat center center fixed;
  background-size: cover;

    }
    .auth-container {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border-radius: 15px;
      padding: 40px;
      width: 380px;
      text-align: center;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
    }
    .h1 {
      margin-bottom: 25px;
      color: #02eeff;
    }
    input {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border: none;
      border-radius: 8px;
      outline: none;
    }
    button {
      width: 100%;
      background: #04c0fa;
      border: none;
      color: #667eea;
      padding: 12px;
      border-radius: 8px;
      font-weight: 600;
      cursor: pointer;
      transition: 0.3s;
    }
    button:hover {
      background: #4e4c7a;
      transform: scale(1.05);
    }
    .auth-switch {
      margin-top: 15px;
      color: #000000;
    }
    a {
      color: #080808;
      text-decoration: underline;
      font-weight: 600;
    }
  </style>
</head>
<body>
  <div class="auth-container">
    <h1>Forgot Password</h1>
    <form action="/forgot-password" method="POST">
      <input type="email" name="email" placeholder="Enter your registered email" required />
      <button type="submit">Send Reset Link</button>
    </form>
    <p class="auth-switch">
      Remember your password? <a href="/login">Login</a>
    </p>
  </div>
</body>
</html>
